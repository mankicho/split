<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="component.member.MemberMapper">
    <resultMap id="authMap" type="component.member.AuthDTO">
        <result property="memberEmail" column="member_email"/>
        <result property="auth" column="auth"/>
    </resultMap>

    <resultMap id="memberMap" type="component.member.MemberDTO">
        <result property="email" column="email"/>
        <result property="pw" column="pw"/>
        <result property="phoneNumber" column="phone_num"/>
        <result property="sex" column="sex"/>
        <result property="bornTime" column="born_time"/>
        <collection property="authList" resultMap="authMap"></collection>
    </resultMap>

    <select id="selects" resultMap="memberMap" parameterType="string">
        SELECT m.email,m.pw,m.phone_num,m.sex,m.born_time,a.member_email,a.auth
        from members as m left outer join members_auth as a on m.email = a.member_email
        <where>
            <if test="value != null">
                m.email = #{value}
            </if>
        </where>
    </select>

    <insert id="registerMember" parameterType="MemberDTO">
        INSERT INTO members(email,pw,phone_num,sex,born_time)
        values (#{email},#{pw},#{phoneNumber},#{sex},#{bornTime})
    </insert>

    <insert id="insertSalt" parameterType="string">
        INSERT INTO members_salt(member_email,salt) values(#{email},#{salt})
    </insert>

    <select id="getSalt" resultType="string">
        SELECT salt from members_salt where member_email = #{email}
    </select>
</mapper>